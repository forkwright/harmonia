/** Audio output settings placeholder — expanded in a later phase. */
export default function AudioSettings() {
  return (
    <section className="space-y-4">
      <h2 className="text-base font-semibold text-gray-100">Audio</h2>
      <div className="bg-gray-900 rounded-lg px-4 py-3">
        <p className="text-sm text-gray-500">
          Audio output configuration is managed via the DSP page.
        </p>
      </div>
    </section>
  );
}
